ПРОБЛЕМА: при редактировании через contenteditable редактируется только одна буква

ПРОБЛЕМА: во время редактирования данные обновляются без подтверждения

ПРОБЛЕМА: во время редактирования невозможно сделать отмену того что пользователь понаписал в поле инпут

ПРОБЛЕМА: поскольку приложение не обеспечивает уникальность flowers, то v-bind:key="flower" в v-for="flower in flowers" будет работать неправильно

КАКНАДО: если значения не уникальны, то нужно понять (для себя) как удалять. либо тот на котором кликнули delete, либо все элементы с таким значением

КАКНАДО: когда ввёл значение в инпут и нажал кнопку ENTER юзер ожидает что что-то призойдёт: если инпут отвечает за добавление, то ENTER должен добавить, если инпут отвечает за редактирование, то ENTER должен отредактировать ESC - отменяет редактирование

КАКНАДО: компонент списка примает на вход список и эмитит событие "список обновлён". компоненту, который подключает компонент списка всё равно по каким причинам обновился список. тоесть родитель компонента списка ДЕЛЕГИРУЕТ задачи отображения и измения (любые) списка на компонент списка

===

боинг начинаем строить со скейта

потому что на скейте сразу можно ехать
